export const PAGE_SIZES = [5, 10, 20, 50];
export const DEFAULT_PAGE_SIZE = 5;

// Note: Used for Table Search Input Field
export const MIN_TEXT_LEN = 3; // Note: Used when searchBy name or any string
export const MIN_NUMBER_LEN = 1; // Note: Used when searchBy ID
