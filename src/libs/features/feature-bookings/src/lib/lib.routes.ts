import { Route } from '@angular/router';
import { FeatureBookings } from './feature-bookings/feature-bookings';

export const featureBookingsRoutes: Route[] = [{ path: '', component: FeatureBookings }];
