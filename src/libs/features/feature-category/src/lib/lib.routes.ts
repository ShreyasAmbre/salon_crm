import { Route } from '@angular/router';
import { FeatureCategory } from './feature-category/feature-category';

export const featureCategoryRoutes: Route[] = [{ path: '', component: FeatureCategory }];
